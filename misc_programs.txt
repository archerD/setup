list of applications not in other files:
    apt  scrcpy
    apt  tmux (config in dotfiles)
    apt  screen
    web  gnirehtet (https://github.com/Genymobile/gnirehtet)
    snap code (visual studio code/vscode)
    snap slack
    web  discord
    web  zoom
    apt  batcat (bat)
    apt  gparted
    apt  latex (texlive-full)
    snap bpytop (remember to grant full permissions, run 'snap info bpytop' for more info)
    apt  ipython3

