list of applications not in other files:
    apt  scrcpy
    apt  tmux
    apt  screen
    web  gnirehtet (https://github.com/Genymobile/gnirehtet)
    snap code (visual studio code/vscode)
    snap slack
    web  discord
    web  zoom
    apt  batcat (bat)
    apt  gparted
    apt  latex (texlive-full)
    snap bpytop (remember to grant full permissions by running sudo snap connect bpytop:{mount-observe,network-control,hardware-observe,system-observe,process-control,physical-memory-observe)
    apt  ipython3

